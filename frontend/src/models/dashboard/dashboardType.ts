export type StatCardProps = {
    icon: React.ReactNode;
    value: string | number;
    label: string;
    accent?: string;
    helper?: React.ReactNode;
    footer?: React.ReactNode;
    className?: string;
};
